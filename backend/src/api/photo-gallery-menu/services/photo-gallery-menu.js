'use strict';

/**
 * photo-gallery-menu service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::photo-gallery-menu.photo-gallery-menu');
